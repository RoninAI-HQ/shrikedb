# Rust build artifacts
/target
**/*.rs.bk
*.pdb

# Cargo
# Note: Cargo.lock IS committed for binaries (which this workspace produces).

# IDEs / editors
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~
.fuse_hidden*
.Trash-*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Environment / secrets
.env
.env.local
*.pem
